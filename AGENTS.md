# Repository Rules

## SaiGame Package Boundary

Never create, modify, move, rename, or delete files under `Assets/SaiGame/`.

Treat `Assets/SaiGame/` as a read-only dependency. Implement project-specific behavior only outside that directory, unless the user explicitly revokes this rule for a specific change.
